import React from 'react';
import 'css/Basket.css';

import NumericInput from 'react-numeric-input';

const Sum = ({ value, price }) => {
  return (
    <div className="sumEnd">
    
    </div>
  );
};

export default Sum;
